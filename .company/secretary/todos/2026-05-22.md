---
date: "2026-05-22"
type: daily
---

# 2026-05-22 (金曜日)

## 最優先（帰宅後PCで対応）

### Firebase Console 作業
- [ ] Firestore に `allowedUsers` コレクションを作成し、以下の4名分のドキュメントを追加する | 優先度: 高
  - ドキュメントID: `taoki0183@gmail.com` / フィールド: `email: "taoki0183@gmail.com"`
  - ドキュメントID: `haruhito3901@gmail.com` / フィールド: `email: "haruhito3901@gmail.com"`
  - ドキュメントID: `m.takada.kp@gmail.com` / フィールド: `email: "m.takada.kp@gmail.com"`
  - ドキュメントID: `momansahaton@icloud.com` / フィールド: `email: "momansahaton@icloud.com"`
- [ ] Firestore セキュリティルールに `allowedUsers` の読み取りルールを追加してデプロイする | 優先度: 高
  ```
  match /allowedUsers/{id} {
    allow read: if request.auth != null
      && (request.auth.token.email == id || request.auth.uid == id);
  }
  ```
- [ ] Vercel に `VITE_FIREBASE_VAPID_KEY` を設定する | 優先度: 高
- [ ] Vercel に `FIREBASE_SERVICE_ACCOUNT_JSON` を設定する | 優先度: 高
- [ ] Vercel に `CLOUDINARY_API_KEY` を設定する | 優先度: 高
- [ ] Vercel に `CLOUDINARY_API_SECRET` を設定する | 優先度: 高

## 通常

- [ ] `.env.local` に `VITE_FIREBASE_VAPID_KEY` を追加してローカルでプッシュ通知を動作確認する | 優先度: 通常
- [ ] `public/mercury-logo.png` を透過背景のPNGに差し替える | 優先度: 通常
- [ ] Firestore ルールをデプロイする（`staff` コレクション追加・`assignees` フィールド許可） | 優先度: 通常

## 余裕があれば

- [ ] `App.tsx` のコンポーネント分割を検討する（2000行超） | 優先度: 低

## 完了

## メモ・振り返り
- ログイン制限はFirestore `allowedUsers` コレクション管理に移行済み
- コレクションが空のままだと誰もログインできないので、帰宅後すぐに追加すること！
- Cloudinaryキー取得: Cloudinary Dashboard → Settings → Security → API Keys
- VAPIDキー取得: Firebase Console → プロジェクト設定 → Cloud Messaging → Web push certificates
- サービスアカウントJSON: Firebase Console → プロジェクト設定 → サービスアカウント → 新しい秘密鍵を生成
